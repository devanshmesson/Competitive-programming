#include <iostream>
using namespace std;

int main()
{
    ios::sync_with_stdio(0);cin.tie(0);cout.tie(0);
    #define int long long int
    int n;
    cout<<1<<endl;
    fflush(stdout);
    cin>>n;
    if(n==2)cout<<3<<endl;
    else cout<<2<<endl;
    fflush(stdout);
    return 0;
}
